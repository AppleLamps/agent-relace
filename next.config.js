/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Environment variables are automatically available in server-side code
  // No need to explicitly pass them via env config
}

module.exports = nextConfig

